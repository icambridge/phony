language: php

php:
  - 5.4
  - 5.5
  - 5.6

before_script:
  - pecl install apcu
  - echo "extension=apcu.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
  - composer install -n

notifications:
  email: false