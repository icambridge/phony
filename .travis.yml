language: php

php:
  - 5.4
  - 5.5
  - 5.6

before_script:
  - sudo pecl install apcu-4.0.6
  - sudo echo "extension=apcu.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
  - composer install -n

notifications:
  email: false